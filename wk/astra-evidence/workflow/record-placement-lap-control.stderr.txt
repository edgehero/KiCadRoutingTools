record NOTE: this placement lap records no --render-json. The read mandates are only auditable through the ledger; without one, a skipped read and an absent trigger look identical later. Attach the render you read, or say in --lever why there was no trigger.
